import { PAGE_SEO_DESCRIPTION, PAGE_SEO_TITLE } from "@/constants";
import { Metadata } from "next";

export const metaData: Metadata = {
  title: PAGE_SEO_TITLE,
  description: PAGE_SEO_DESCRIPTION,
};
